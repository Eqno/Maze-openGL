double r = sqrt(actor[0].x*actor[0].x+actor[0].y*actor[0].y+actor[0].z+actor[0].z);
    // printf("%.2f\n", r);
    // for (auto i: wallEx)
    //     for (auto j: i)
    //         if (sqrt(j.x*j.x+j.z*j.z) < r) return false;